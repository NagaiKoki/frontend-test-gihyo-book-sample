// Prettier デモ用: 4スペースインデント・セミコロンなし・ダブルクォートの整形前コード（本書 コード3.1 相当）
// `npx prettier --check "examples/**/*.ts"` で違反を確認し、`--write` で整形してみてください。
const sum = (x: number, y: number) => {
    return x + y
}

const message = "hello world"
